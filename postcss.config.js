// ⚠️ NÃO NECESSÁRIO COM TAILWIND 4 + PLUGIN VITE
// O @tailwindcss/vite plugin é responsável por processar o Tailwind
// Este arquivo é mantido apenas para referência e não é usado

export default {
  plugins: {
    // '@tailwindcss/postcss': {}, // Removido - Tailwind 4 com Vite não precisa
    'autoprefixer': {},
  },
}
